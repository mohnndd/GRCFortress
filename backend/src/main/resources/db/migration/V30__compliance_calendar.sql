-- No new tables needed: compliance calendar is an aggregate view over existing tables.
-- This migration is a placeholder to reserve the version slot.
-- The /api/v1/calendar/events endpoint queries existing tables directly.
SELECT 1;
